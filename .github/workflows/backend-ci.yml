name: Backend CI

on:
  push:
    branches: [ "main", "Backend-Dev" ]
    paths:
      - 'backend/**'
  pull_request:
    branches: [ "main", "Backend-Dev" ]
    paths:
      - 'backend/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('backend/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore - GSN.Domain
        run: dotnet restore backend/src/GSN.Domain/GSN.Domain.csproj

      - name: Restore - GSN.Infrastructure
        run: dotnet restore backend/src/GSN.Infrastructure/GSN.Infrastructure.csproj

      - name: Restore - GSN.Application
        run: dotnet restore backend/src/GSN.Application/GSN.Application.csproj

      - name: Restore - GSN.Agents
        run: dotnet restore backend/src/GSN.Agents/GSN.Agents.csproj

      - name: Restore - GSN.Api
        run: dotnet restore backend/src/GSN.Api/GSN.Api.csproj

      - name: Build - GSN.Domain
        run: dotnet build backend/src/GSN.Domain/GSN.Domain.csproj --no-restore --configuration Release

      - name: Build - GSN.Infrastructure
        run: dotnet build backend/src/GSN.Infrastructure/GSN.Infrastructure.csproj --no-restore --configuration Release

      - name: Build - GSN.Application
        run: dotnet build backend/src/GSN.Application/GSN.Application.csproj --no-restore --configuration Release

      - name: Build - GSN.Agents
        run: dotnet build backend/src/GSN.Agents/GSN.Agents.csproj --no-restore --configuration Release

      - name: Build - GSN.Api
        run: dotnet build backend/src/GSN.Api/GSN.Api.csproj --no-restore --configuration Release