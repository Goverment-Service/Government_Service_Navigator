# Orchestration

Wires the four agents into the plan → check → act → validate → approve
pipeline, and calls the human-approval hook before an application is
submitted into the officer's queue (see project plan §2, "High-impact
action requiring human approval").

Not implemented yet — framework/orchestration method is tracked as an ADR
(project plan §5.2, decision 3).
